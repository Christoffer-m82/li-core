# Finance and Calendar staging release: `a7601b7`

## Status and scope

**Deployed to staging on 2026-09-07; acceptance remains partial.** Commit
`a7601b7c3dfbcc1e8b410fc1b1e06d56c760d5eb` is the coordinated backend and web source for the
private Finance and Calendar workspaces and the Specialist Workspace composer. Migration 042 is
applied. Backend `li-os-release-a7601b7` and web `li-os-web-release-a7601b7` are Ready and each serve
100% of staging traffic.

This release does not prove physical Android phone/tablet, installed-Windows PWA, owner, stability,
or automatic market-price acceptance. Follow the [Finance and Calendar architecture](../../system/FINANCE_CALENDAR_ARCHITECTURE.md),
[migration workflow](../MIGRATION_WORKFLOW.md), and [deployment workflow](../DEPLOYMENT_WORKFLOW.md).

## Backup, restore, and migration evidence

- A fresh pre-042 staging backup passed the schema-0.41 source gate, authenticated encrypted-archive
  validation, and `pg_restore` catalogue validation. The protected archive remains outside Git at
  `output/release-backups/li-os-pre-042-20260907T154732Z.pgdump.liosenc`; SHA-256 is
  `8bd8990b72a2739b4164869897ca3ceb11834a2d48d572dce991574ae3fee6f0` and the catalogue contains
  1,250 entries. The passphrase was entered only through masked local prompts.
- The complete archive restored into an exactly named localhost-only disposable PostgreSQL target at
  schema 0.41. The drill found one active owner, 23 canonical memory records, 62 canonical tables,
  and completed in 2.18 seconds. These bounded counts do not expose personal record values.
- The exact migration-042 SHA-256
  `6b8f905bc0d3bb2b2a9355d37e8ad6c330f63ea7f22d8ff51a06aed9f2dbe0c0` passed isolated rehearsal.
  Schema 0.42, Li/backend access, reviewed denials, and synthetic create/list/update/duplicate/archive
  behavior passed. The synthetic transaction was rolled back.
- Migration 042 was then applied once to staging. Post-migration checks reported schema 0.42,
  unchanged protected counts, Li/backend access, and the reviewed authority denials. No provider
  call or personal-record value was read during migration. The exact disposable rehearsal container
  was removed after review.

## Application rollout evidence

- Both images were built from a tracked-only archive of the reviewed commit. Protected backup paths
  and the untracked specialist-thumbnail ZIP were not in the build context.
- Backend image manifest:
  `europe-west1-docker.pkg.dev/li-os-staging/li-os/li-os@sha256:d35bc6615e2f85a698d85abaafe7fc0bc20c1dcc3fe5fa930f8d0b276cb9005e`.
- Web image manifest:
  `europe-west1-docker.pkg.dev/li-os-staging/li-os/li-os-web@sha256:3b2864e4bf5c7d8ac7edda6351f726c1a355e43c167e3b175befdd9242083d87`.
- Separate zero-normal-traffic candidates passed readiness before promotion. The owner entered the
  existing Li API token only through a masked local prompt; authenticated backend readiness and
  restricted database health reported schema 0.42.
- After promotion, the backend still uses
  `li-os-runtime@li-os-staging.iam.gserviceaccount.com`, has 18 numeric secret-version references,
  and has no `allUsers` binding. Public backend health is denied with 403 and an IAM-authenticated
  health request returns 200.
- The web still uses `li-os-web-runtime@li-os-staging.iam.gserviceaccount.com`, has five numeric
  secret-version references, retains its intended public invoker binding, and returns 200 for public
  health. Signed-in Home loaded all four live sections and reported Li online.
- A bounded two-hour log review found no backend errors and no unexpected web errors. Six web
  `/api/profile/photo` requests returned the documented 503 fallback while the optional private photo
  service remains unconfigured; the UI correctly retained the `CM` initials fallback.

Current application rollback targets are backend `li-os-release-0fc39a7` and web
`li-os-web-release-7527434`, both still Ready. Application rollback does not reverse schema 0.42 or
remove portfolio rows. Database recovery remains a separately reviewed restore or corrective
migration.

## Workspace acceptance

- Signed-in staging exposed **My Finances** and **Calendar** from the expected web release.
- Calendar rendered a Monday-to-Sunday week, a separate month overview, weekend distinction, and the
  governed read-only explanation. Its provider-backed refresh failed safely with
  `Calendar unavailable: Calendar search failed; no calendar state was changed.` No Calendar write
  occurred. Provider-backed display therefore remains open rather than being relabelled as a pass.
- A clearly named synthetic Avanza holding completed create and edit through the live owner UI. The
  calculated cost basis, current value, unrealized amount, percentage, currency, and price timestamp
  updated consistently. The record remains active pending its separately confirmed UI archive; no
  broker credential, market quote, trade, memory write, or specialist share was used.
- The Specialist composer assets were present in the promoted image and had already passed local
  browser and CI coverage. A separate signed-in interaction through that composer was not created
  merely to obtain release evidence.

## Cost and residual limitations

The pre-rollout Google Cloud billing report showed EUR 263.29 promotional credit remaining, EUR 0.00
charged for 1–6 September 2026, and a EUR 0.00 September forecast. This dated evidence supported the
bounded rollout without a purchase, upgrade, billing change, or provider activation; it is not a
guarantee of future cost state.

OM-010 remains open for provider-backed Calendar display, completion of the synthetic archive check,
physical Android phone/tablet and installed-Windows owner acceptance, and stability observation.
Automatic quotes, FX conversion, realized gain/loss, tax reporting, broker synchronization, and
trading remain outside this release.
