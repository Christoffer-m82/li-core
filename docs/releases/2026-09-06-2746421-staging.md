# Staging release record: `2746421`

## Status

**Migration 040 and the matching backend and web release are live in staging. Core read-only smoke
checks passed; physical-device, live-model conversation, mutation, and stable-use acceptance remain
open.** This record separates repository evidence from operator-verified external state and does not
claim production or device readiness.

Follow the authoritative [deployment workflow](../DEPLOYMENT_WORKFLOW.md),
[migration workflow](../MIGRATION_WORKFLOW.md), and
[security boundaries](../SECURITY_BOUNDARIES.md).

## Release identity

| Field | Recorded value |
| --- | --- |
| Environment | Google Cloud project `li-os-staging`, region `europe-west1`; Supabase staging database |
| Application source | Merge commit `2746421` from pull request 61; this commit is an ancestor of `main` at `896e1e1` |
| Database change | Immutable `040_owner_memory_proposal_inspection.sql`, SHA-256 `067c0602bc6b573741b144d20d6d4d30fbc071292ad187341cee0df0c39ef381` |
| Backend image | Release tag `release-2746421`; deployed digest `sha256:d78f78bc8ea509965336fa83a25cc74326186d6e694968fd02abcea35a0852e0` |
| Web image | Release tag `release-2746421`; deployed digest `sha256:6ac93fd6bafb21c0cb275ada498649df228bc989a2c8e6658e73c3870d8bb8d8` |
| Approval | Owner-authorized staging migration and deployment in the current Codex task on 2026-09-06 |
| Expected impact | Narrow staging schema and application rollout; no intentional deletion, IAM, secret, scheduler, or billing change |

## Prerequisite evidence

- The replacement encrypted backup authenticated, passed `pg_restore` archive validation, and then
  passed an isolated schema-0.39 restoration and canonical retrieval drill. Its SHA-256 is
  `90523b35d05bf3d486dd454562f13cffb226e21e26eec3859efec217d5493128`; the passphrase remains
  owner-controlled and is not stored in the repository.
- The migration preflight found schema 0.39, exactly one active owner, and no prior migration-040
  record. The owner entered the database password only into a masked local prompt.
- Migration 040 committed successfully at `2026-09-06T11:26:07Z`. Post-migration validation found
  schema 0.40, allowed the owner function, and denied Li, Theo, backend, and direct table access.
- `main` at `896e1e1` passed both current GitHub workflows: all 12 CI checks and documentation checks
  completed successfully. The release application commit is an ancestor of that reviewed state;
  later changes are documentation, tests, migration-history metadata, and deployment hardening rather
  than additional backend or web application behavior.
- A read-only billing review showed the bounded rollout covered by existing promotional credit, with
  no current-month charge or forecasted charge. No purchase, plan change, paid overage, billing
  setting, or additional billable resource was created.

## Deployment and rollback state

Both revisions became Ready and ContainerHealthy at zero normal traffic before promotion. Traffic was
moved backend first and web second. The former live release remains directly addressable as the first
application rollback target.

| Purpose | Revision | Traffic or route state |
| --- | --- | --- |
| Backend live | `li-os-release-2746421` | 100%; tagged `candidate-2746421` |
| Backend rollback | `li-os-release-c3f2d51` | 0%; tagged `review-c3f2d51` and retains its earlier candidate route |
| Web live | `li-os-web-release-2746421` | 100%; tagged `candidate-2746421` |
| Web rollback | `li-os-web-release-c3f2d51` | 0%; tagged `review-c3f2d51` and retains its earlier candidate route |

The dedicated service accounts, scaling configuration, and numeric Secret Manager version references
were inherited unchanged. Backend IAM remained private and web IAM remained public only at the BFF
boundary. No secret value was read or changed.

## Live validation

- Direct zero-traffic candidate checks returned `ok` from backend and web health. An unauthenticated
  request to the web owner-memory route returned 401.
- After backend promotion, its live health returned `ok`. The still-live preceding web revision then
  loaded all authenticated dashboard sections through the new backend, including live database
  readiness.
- After web promotion, live health returned `ok` on five consecutive checks. A signed-in owner browser
  session loaded Home, Backend Overview, conversation history, canonical-memory search, and the new
  **Memory awaiting review** region.
- A later signed-in, read-only specialist smoke check opened Marco from the Home specialist card and
  confirmed that **Workspace** is the default specialist-activity tab. The saved three-party
  conversation loaded with distinct owner, specialist, and Li attribution; **History** exposed the
  recorded request, response, findings, assumptions, questions, and evidence metadata; and
  **Statistics** rendered bounded consultation, outcome, collaboration, attribution, response-time,
  and daily-activity measures without inventing unavailable scores.
- The specialist portrait dialog displayed Marco's registry name and role, exposed the selected
  full-resolution portrait asset, focused its Close control, and closed without changing data.
  Settings also loaded the current appearance, installation, voice/privacy, retention, and profile
  sections. The optional profile-photo boundary remained honestly unavailable and retained the
  initials fallback; no setting was changed.
- The owner proposal list reported one outstanding suggestion and rendered its confirmation state
  read-only. The validation did not confirm, mutate, correct, forget, or expose a proposal identifier.
- Request logs recorded successful traffic for both new revisions. No backend ERROR-level entry was
  found. One web `/api/profile/photo` request returned 503 during promotion; this is the documented
  fail-closed behavior of the optional, unconfigured profile-photo service. The UI used initials,
  subsequent health and page loads passed, and no further ERROR-level entry appeared after promotion.
- No live-model chat was generated for smoke testing because separate Anthropic API cost coverage is
  not established by the owner's consumer subscription. Provider-backed conversation acceptance
  therefore remains open instead of being relabelled as passed.
- Pull requests 66 and 67 recorded this release and updated its physical-device target. Each passed
  all 12 pull-request checks before merge, and both documentation and full CI completed successfully
  on `main` through merge commit `4feec54`. These later documentation commits do not change the
  deployed application identity.

## Remaining acceptance

This release closes the schema-0.40 deployment and read-only owner-proposal inspection portions of the
[personal-use checklist](../PERSONAL_V1_ACCEPTANCE.md). It does not close:

- live English/Swedish conversation and specialist-routing smoke tests;
- authorized correction, forgetting, and other data-integrity mutation journeys;
- provider-specific Calendar, Gmail, research, and task journeys;
- Android phone, Android tablet, Windows installed-PWA, screen-reader, and actual 200% zoom checks;
- normal-use stability observation; or
- OM-003 provider evaluation, architecture, implementation, deployment, and enhanced-voice owner
  acceptance.

Application rollback is a traffic shift to the recorded `release-c3f2d51` revisions. Schema 0.40 is
forward-only migration history and is not reverted by application rollback; follow the migration
workflow for any data-preserving corrective change.
