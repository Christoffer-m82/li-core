# Staging release record: `8831381`

## Status

**Migration 041 and the matching backend and web release are live in staging. The protected schema,
runtime identities, signed-in read paths, recovery UI, logs, and application rollback path passed the
bounded checks below. Provider-backed bilingual mutation, physical-device, owner, and stable-use
acceptance remain open.**

This record separates repository, operator-verified external, and still-unverified evidence. Follow
the authoritative [deployment workflow](../DEPLOYMENT_WORKFLOW.md),
[migration workflow](../MIGRATION_WORKFLOW.md), and
[security boundaries](../SECURITY_BOUNDARIES.md).

## Release identity

| Field | Recorded value |
| --- | --- |
| Environment | Google Cloud project `li-os-staging`, region `europe-west1`; Supabase staging database |
| Application source | Merge commit `8831381ac9e26fe913b8aacb5963c0c261607564`; pull requests 73–76 are included |
| Database change | Immutable `041_chat_memory_effect_fencing.sql`, SHA-256 `582cbd7be238ee1af20ef4e4725ffae2c6b683aa43cf5f81ac80c028b186960a` |
| Backend image | Published manifest `sha256:3d15f71efa847e3e70c802d4b528c2fd1b1280fff4d186000a24ff5e1d2d94b4`; Cloud Run resolved digest `sha256:9eaff79d69f5b7f9fdc80e58b5ede10100f20c22cc69cc26f6b5fe49c75fc545` |
| Web image | Published manifest `sha256:e4c09e326e08e5912d1d3d5117f651b4ee47b5469fc4f1e7f2d5b94bddb6ea2b`; Cloud Run resolved digest `sha256:f9f73fad95781cddb7118ce86229746f6f6ff541deaf45fbd3bd35b564a3e836` |
| Approval | Owner-authorized staging migration and deployment in the current Codex task on 2026-09-06 |
| Scope | Schema 0.41, backend, and web only; no IAM, secret, scheduler, native-gateway, provider, billing, or personal-record change |

## Backup, restore, and migration evidence

- Read-only preflight proved schema 0.40, exactly one active owner, no later schema, and migration
  041 absent before export.
- The fresh encrypted pre-041 backup authenticated and passed archive validation. Its SHA-256 is
  `1ca3ca103da85f852c4ac7600b6382021659471c5df06b7a7305b315656cb7a5`; the encryption passphrase
  remains owner-controlled and is not stored in the repository.
- A fresh isolated PostgreSQL target restored the backup at schema 0.40 with one owner, 23 canonical
  memory records, and 62 canonical tables. Retrieval validation completed in 1.5 seconds.
- Migration 041 then passed on that restored target. It allowed the expected Li/backend authorities
  and denied Theo, owner-confirmation, retention, anonymous, authenticated, service-role, direct
  turn-table update, and lingering schema-create paths.
- The owner applied migration 041 once through the masked operator runner. Staging validation proved
  schema 0.41, unchanged protected counts, expected Li/backend access, and all reviewed denials.
  No secret was copied into chat or the repository.

The encrypted backup is recovery evidence, not a database rollback command. Application traffic can
be rolled back independently; schema 0.41 remains forward-only migration history.

## Build, review, and cost evidence

- Backend tests passed 1,068 cases and Ruff passed. Frontend checks passed 93 Python tests, 85 Node
  browser tests, Ruff, and compile validation. The complete migration manifest through 0.41 passed
  in the CI-pinned disposable PostgreSQL environment.
- Pull request 76 passed all 12 required checks before merge. All 12 post-merge checks then passed on
  `8831381`, including container builds, migration validation, native checks, Markdown validation,
  and the repository secret/file audit.
- The backend and web images were built locally from the synchronized merge commit using pinned base
  image digests, then pushed under the immutable `release-8831381` tag. No Cloud Build job was used.
- A read-only billing check after publication and rollout still showed €263.32 of €263.35 promotional
  credit remaining, €0.00 charged for the reporting period, and a €0.00 forecast. No purchase,
  upgrade, trial, overage, billing change, or charge outside the existing coverage was started.

## Deployment, validation, and rollback

Backend and web were each deployed as zero-normal-traffic candidates before promotion. The backend
was promoted first; the preceding web revision then loaded all signed-in dashboard sections against
it. The web revision was promoted only after its candidate checks passed.

| Purpose | Revision | State |
| --- | --- | --- |
| Backend live | `li-os-release-8831381` | Ready; 100% traffic; candidate route retained |
| Backend rollback | `li-os-release-2746421` | Ready; 0% traffic; candidate route retained |
| Web live | `li-os-web-release-8831381` | Ready; 100% traffic; candidate route retained |
| Web rollback | `li-os-web-release-2746421` | Ready; 0% traffic; candidate route retained |

- Backend kept `li-os-runtime@li-os-staging.iam.gserviceaccount.com`, all 18 secret references stayed
  pinned to numeric versions, and no `allUsers` binding appeared. Unauthenticated Cloud Run access
  returned 403; an authenticated `/health` request returned `ok`; `/ready` without Li's separate API
  token returned 401.
- Web kept `li-os-web-runtime@li-os-staging.iam.gserviceaccount.com`, all five secret references stayed
  numerically pinned, and public access remained limited to the intended browser/BFF boundary. Its
  candidate and promoted `/health` returned `ok`.
- The signed-in owner browser loaded Home and its live data sections, then opened a specialist directly
  into Workspace and loaded the saved three-party conversation. This was read-only; no conversation,
  memory, proposal, profile, or setting was changed.
- No backend ERROR entries and no unexpected web ERROR entries were found for the new revisions. Two
  web requests to `/api/profile/photo` returned the documented 503 fallback because the optional
  private profile service is deliberately unconfigured; the initials fallback remained in use. The
  same fail-closed condition existed on the prior release and is not treated as profile activation.

Rollback safety clarification (2026-09-06): the retained `release-2746421` revisions are
schema-compatible candidates, not privacy-safe fallbacks. That backend predates the historical-
recall privacy, memory-effect fencing, and private-proposal corrections. Schema 0.41 alone does
not make its application paths safe. Do not return affected chat or memory-write traffic to it
merely because application validation fails. Follow the
[authoritative rollback safety gate](../../README.md#rotation-and-rollback); if no safe target
exists, obtain an authorized containment or corrected-release plan. No traffic was changed by
this clarification. Do not remove migration 041 or retry uncertain memory effects as a rollback
shortcut.

## Remaining acceptance

This release establishes that the privacy, recovery, and private-proposal guards are merged, schema
compatible, and deployed together. It does not establish:

- provider-backed English/Swedish routing, response, or memory-mutation smoke acceptance;
- inspection or reclassification of previously affected personal records;
- private-source proposal support, which remains fail-closed by design;
- correction, forgetting, uncertain-effect reconciliation, or other authorized live mutation journeys;
- physical Android phone/tablet, installed-Windows PWA, microphone, screen-reader, 200% zoom, owner,
  or stable-use acceptance; or
- OM-003 provider selection, implementation, deployment, or enhanced-voice acceptance.

OM-003 therefore remains planned and dependency-gated. No voice or model provider was activated and
no metered provider call was made for this release.
