# Public OAuth information web release: `131034f`

## Status and scope

**Web deployed to staging on 2026-09-08; OAuth branding and publication are complete while the
credential repair remains open.** Merge commit
`131034f810820ba78b0ecb521e3245aebb710d3d` adds fixed public About, Privacy Policy, and Terms
documents and links them from the signed-out shell. Web revision `li-os-web-release-131034f` is
Ready and serves 100% of staging traffic. Backend remains `li-os-release-db1d17c`; the database
remains at schema 0.42.

The web deployment itself did not change Google OAuth publishing status, OAuth consent, client
credentials, refresh authorization, backend code, database state, IAM, scheduler resources,
providers, billing, or personal records. The Calendar refresh-grant failure recorded in the
[diagnostic backend release](2026-09-08-db1d17c-staging.md) is therefore not repaired by this
deployment.

## Review, build, and cost gates

- [PR #97](https://github.com/Christoffer-m82/li-core/pull/97) merged after all 12 pull-request
  checks passed; all 12 checks on the merge commit also passed.
- Local frontend evidence recorded Ruff, 100 Python tests, 93 Node browser tests, compileall, the
  tracked-file secret audit, Markdown link validation, and the staged whitespace check passing. The
  visible upstream Starlette/AnyIO warning remained unsuppressed.
- The complete nine-file change was reviewed. Anonymous access expands only to three fixed,
  data-free documents. Application routes and `/api/ready` retain their existing session boundary.
- The image was built from a tracked-only archive of the exact merge commit. Protected backup paths
  and the untracked specialist-thumbnail ZIP were absent from the build context.
- The published multi-platform manifest is
  `sha256:00639d4a98138579d5337006d7d7919a5896a0e86afaa3d1457d23e127c4049a`; Cloud Run resolved
  the runtime image to
  `sha256:53ca6f22d284004eddb1bbddfc898a4ab30a82fd68eb0528de01b43965d7d664`.
- The same-day [Calendar diagnostic release](2026-09-08-db1d17c-staging.md) records EUR 263.25 of
  promotional credit remaining, EUR 0.00 charged for 1-7 September, and a EUR 0.00 September
  forecast. The owner authorized this bounded rollout against existing promotional credit only,
  with no additional payment, purchase, upgrade, overage, or billing change.

## Candidate and promotion evidence

- Zero-normal-traffic candidate `li-os-web-release-131034f` was Ready while
  `li-os-web-release-a7601b7` continued serving 100% of normal traffic.
- The candidate retained `li-os-web-runtime@li-os-staging.iam.gserviceaccount.com`, all five existing
  numeric secret-version references, and the intended public web invoker binding. No IAM or secret
  reference changed.
- Candidate `/health`, `/`, `/about`, `/privacy`, and `/terms` returned 200. Security headers were
  present; `/api/ready` returned 401 without a signed-in session. The public documents cross-link,
  and the Privacy Policy names the Calendar events scope and its data, use, sharing, retention,
  security, approval, and revocation boundaries.
- No candidate ERROR log entries were found before promotion. The validated immutable revision was
  then promoted to 100% staging traffic.
- Post-promotion checks repeated 200 responses for health, the signed-out shell, and all three public
  documents; unauthenticated `/api/ready` remained 401. Traffic reported 100% on
  `li-os-web-release-131034f`, and no ERROR entries were found for the new revision.

## Rollback and remaining gates

`li-os-web-release-a7601b7` remains Ready at 0% and is the immediate web rollback target. Web
rollback would remove the public OAuth documents but would not change backend `db1d17c`, schema
0.42, Google OAuth settings, credentials, or Calendar data.

The deployed URLs are:

- About: `https://li-os-web-7gyegrz7vq-ew.a.run.app/about`
- Privacy Policy: `https://li-os-web-7gyegrz7vq-ew.a.run.app/privacy`
- Terms: `https://li-os-web-7gyegrz7vq-ew.a.run.app/terms`

## OAuth branding and publication evidence

After the web rollout, the owner separately authorized the Google OAuth configuration change. The
three deployed URLs above were saved as the application's home page, Privacy Policy, and Terms of
Service. The authorised-domain list was verified to contain only
`li-os-web-7gyegrz7vq-ew.a.run.app`; the incorrect `www.googleapis.com` entry was absent. The
external app was then changed from Testing to In production, and the Audience page displayed
`In production` afterward.

No OAuth client, credential, scope, provider, IAM, billing, database, backend, scheduler, or
personal-record change was made in that operation. Publication can still present Google's
unverified-app warning depending on the requested scopes and verification state; it is not proof
that Calendar authorization succeeds.

Owner consent, replacement numeric client-secret and refresh-token versions, a backend revision
referencing them, and a newly authorized bounded Calendar read remain separately protected actions.
Provider-backed Calendar, physical-device, owner, and stability acceptance remain open. This record
makes no claim that those gates passed.

## Dated follow-up

Those Calendar credential and bounded-read gates were subsequently completed by the separately
authorized [2026-09-09 Calendar configuration release](2026-09-09-calendar-v2-staging.md). This does
not change the historical scope of the web release or establish physical-device, owner, or stability
acceptance.
