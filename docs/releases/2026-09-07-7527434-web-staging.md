# Web staging release record: `7527434`

## Status

**The five additional appearances and the visual theme gallery are live in the existing Li OS web
staging service. Repository, CI, candidate, promotion and public health/asset checks passed. Signed-in
owner, physical-device and assistive-technology acceptance remain open.**

This record separates repository evidence from operator-verified external state. Follow the
[deployment workflow](../DEPLOYMENT_WORKFLOW.md) and the component
[staging guidance](../../frontend/README.md#staging-deployment).

## Release identity

| Field | Recorded value |
| --- | --- |
| Environment | Google Cloud project `li-os-staging`, region `europe-west1` |
| Application source | Merge commit `752743403f12f50c660d77b1cda5d2514d44a384`; pull request 88 |
| Scope | Web BFF/static application only; no backend, database, IAM, secret, scheduler, provider or billing change |
| Published image | `europe-west1-docker.pkg.dev/li-os-staging/li-os/li-os-web@sha256:13e0aa4caea2c7ea667d15e12ad2fe772e6bebe1ec1e580f4ac4e2725f0bf0c5` |
| Cloud Run resolved image | `europe-west1-docker.pkg.dev/li-os-staging/li-os/li-os-web@sha256:0794af89d9161adce0c51a3551a500518ef57f674f1698be5ef73a7666d81f9c` |
| Live revision | `li-os-web-release-7527434`, 100% staging traffic |
| Immediate web rollback | `li-os-web-release-8831381`, Ready at zero normal traffic |
| Approval | Owner-authorized web staging deployment in the current Codex task on 2026-09-07 |

## Review, validation and cost evidence

- The complete web runtime diff from `8831381` to `7527434` contained only the reviewed appearance
  library and gallery change. It adds Nordic Linen, Fjord, Midnight Brass, Rosewood and Porcelain &
  Ink while preserving Dark, Light, Forest, Auto and custom themes.
- Pull request 88 passed all 12 required checks before merge. All 12 post-merge checks passed,
  including frontend, backend, container, native, migration, documentation and secret/file audits.
- Local validation passed 87 Node browser-logic tests, 93 frontend Python tests, Ruff and compileall.
  An isolated Chrome run covered keyboard selection, filtering, persistence and horizontal overflow
  at 390×844, 800×1280 and 1440×900. This remains simulated evidence.
- The image was built locally from a tracked-only archive at the merge commit using the pinned base
  image digests. The resulting container runs as the non-root `liweb` user and contained all five
  expected theme records and the gallery implementation before publication. No Cloud Build job ran.
- Immediately before deployment, the signed-in Google Cloud billing report showed €263.30 of
  €263.35 promotional credit remaining, €0.00 spent for the current reporting period and a €0.00
  monthly forecast. The same values remained visible after promotion. No purchase, upgrade, trial,
  overage or billing change was made.

## Candidate, promotion and rollback

- `li-os-web-release-7527434` became Ready behind the `candidate-7527434` route at zero normal
  traffic. The live service remained `li-os-web-release-8831381` throughout candidate validation.
- Candidate `/health`, the five expected theme names in `/assets/themes.js`, the gallery code in
  `/assets/app.js`, and service-worker cache `li-shell-v23` passed.
- The runtime identity remained `li-os-web-runtime@li-os-staging.iam.gserviceaccount.com`. The five
  secret references remained numerically pinned: API token version 1, owner API token version 1,
  session secret version 2, Google client ID version 3 and Google client secret version 2. No secret
  value was read, displayed, copied or changed.
- The intended public web Cloud Run Invoker binding was unchanged. Candidate logs contained no
  ERROR entries before promotion.
- After promotion, the canonical staging URL returned healthy, served all five new theme records and
  gallery code, and reported `li-shell-v23`. Cloud Run reported exactly one 100% traffic revision:
  `li-os-web-release-7527434`. No new revision ERROR entries were found.
- The backend remained `li-os-release-0fc39a7` at 100% traffic. The database was not accessed or
  modified; schema 0.41 is the last recorded evidence, not a new verification by this release.

If a web-only defect appears, return traffic to `li-os-web-release-8831381` after rechecking its Ready
state and current safety. This does not roll back the database or backend. Do not use a web traffic
change to infer a database change.

## Remaining acceptance

The release does not establish:

- a signed-in Settings → Appearance walkthrough or owner preference acceptance;
- physical Android phone, Android tablet or installed-Windows PWA behavior;
- screen-reader, 200% zoom or other assistive-technology acceptance;
- custom-theme transfer between physical devices; or
- a new backend, database, provider, voice or personal-data acceptance result.

An installed PWA or open browser tab may briefly retain the preceding service-worker cache. Reopen or
refresh Li OS so `li-shell-v23` can activate before judging the new gallery. Theme selection remains
local to each browser/device.
